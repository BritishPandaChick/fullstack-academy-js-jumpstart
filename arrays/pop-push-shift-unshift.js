// STEP 1: Create an array and assign it to a variable.

var array = [];



console.log(array, 'STEP 1')
// NOTE: (replace "array" with the name of your variable in the console.log, comment the code to view the output for each step in the console)

// STEP 2: Use the `push` method to add 5 values into your Array

array.push(6,7,8,9,10);

console.log(array, 'STEP 2')

// STEP 3: Use the `unshift` method to add 5 values to the front of your Array.

array.unshift(11,12,13,14,15);



console.log(array, 'STEP 3')


// STEP 4: Use the `pop` method to remove 2 values from the back of your Array.

array.pop();
array.pop();


console.log(array, 'STEP 4')

// STEP 5: Use the "shift" method to remove 2 values from the front of your Array.

array.shift();
array.shift();

console.log(array, 'STEP 5')
